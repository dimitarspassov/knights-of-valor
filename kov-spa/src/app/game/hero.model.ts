export class HeroModel {
  constructor(public name?: string, public level?: number, public gold?: number,
              public health?: number, public strength?: number,
              public stamina?: number, public defense?: number) {

  }
}
